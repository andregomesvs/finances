rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // O navegador não acessa dados financeiros diretamente nesta etapa.
    // O backend usa IAM e validação de usuário antes de chamar o Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
